import { defaultStyles } from '@/styles'
import { View, Text } from 'react-native'

const SongScreens = () => {
	return (
		<View style={defaultStyles.container}>
			<Text>Song Screens</Text>
		</View>
	)
}

export default SongScreens;
